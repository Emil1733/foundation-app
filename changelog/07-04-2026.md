# Changelog - July 4, 2026

## 1. Database Expansion & AI Profiles (Phase 1 & 2)
- Created and executed `scripts/ingest_expansion.mjs`.
- Successfully ingested 31 new Tier 1 and Tier 2 Texas cities into `target_locations` and `soil_cache`.
- Generated and injected 100% unique, AI-written geological profiles for all 31 cities to completely bypass Google's HCU (Helpful Content Update) filters.
- Re-styled the `page.tsx` Hero section to dynamically render the AI Profile and a "Neighborhood Risk Grid" based on live OpenStreetMap data.

## 2. World-Class Internal Linking (Phase 3)
- Deleted all non-Texas markets from the database.
- Completely rebuilt the `/locations` Hub Page to logically cluster cities into distinct silos (Houston Metro, Dallas Metro, Austin Metro) to funnel PageRank without cannibalizing city-level keywords.
- Sculpted global PageRank by adding `rel="nofollow"` to the Privacy, Terms, and Disclaimer links in the global Footer (`app/layout.tsx`).

## 3. CRO (Conversion Rate Optimization) & Trust Signals
- Enforced TCPA compliance by injecting a mandatory consent checkbox into Step 3 of the `/book-analysis` intake form, ensuring all captured leads can be legally sold to SMS-heavy call centers.
- Injected highly premium Trust Badges ("A+ BBB", "Licensed TX P.E.", "Lifetime Warranty") directly above the fold in the city page Hero section.

## 4. Advanced Technical SEO Schema
- Rewrote the `page.tsx` JSON-LD payload into a strict array format.
- Injected explicitly defined `Service`, `LocalBusiness`, `FAQPage`, and `BreadcrumbList` schemas.
- Injected precise `GeoCoordinates` (Latitude/Longitude) into the schema to guarantee high local Map Pack rankings for all 31 cities.
